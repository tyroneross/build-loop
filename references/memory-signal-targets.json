{
  "schema_version": "1.0",
  "declared": "2026-09-01",
  "purpose": "Executable targets for the memory usefulness signal. Prose targets rot silently; these are checked by `python3 scripts/memory_store_stats.py --check-targets`, so a claim about progress is a command, not an assertion.",
  "scope_rule": "Every metric is computed on the CLEAN tier only (schema_version != 1.0 AND source == 'runtime'). 40,843 of 41,145 read rows are legacy test fixtures; blending them measures the test suite. memory_health.py and memory_store_stats.py share tier_of() so the two cannot disagree.",
  "metrics": {
    "hit_rate": {
      "definition": "clean reads returning >=1 candidate / clean reads",
      "computed_from": "memory-read rows, non-empty memory_ids_seen",
      "baseline_2026_09_01": 0.737,
      "target": 0.9,
      "target_rationale": "A quarter of queries returning nothing is mostly retrieval failure, not genuine absence. Not 1.0: some questions legitimately have no stored answer, and forcing that to 1.0 would reward returning junk.",
      "ceiling": 1.0,
      "falsifier": "If hit rate rises while the join-confirmed usefulness rate falls, the gain is junk candidates, not better retrieval. Check both together."
    },
    "joinable_rate": {
      "definition": "clean reads carrying returned_paths / clean reads",
      "computed_from": "memory-read rows, non-empty returned_paths",
      "baseline_2026_09_01": 0.263,
      "baseline_among_reads_with_results": 0.357,
      "target": "equal to hit_rate",
      "target_rationale": "A zero-result read has no paths to carry, so hit_rate IS the ceiling. Any gap between joinable_rate and hit_rate is a caller not passing the join key. Judge rollout on the among-results figure; the headline understates it.",
      "ceiling": "hit_rate",
      "falsifier": "A caller added later that omits returned_paths reopens the hole silently. scripts/test_memory_join_contract.py audits callers structurally to prevent that."
    },
    "session_rate": {
      "definition": "clean reads carrying session_id / clean reads",
      "computed_from": "memory-read rows, non-empty session_id",
      "baseline_2026_09_01": 0.03,
      "target": 1.0,
      "target_rationale": "Resolved automatically from the runtime environment, so anything below 1.0 is a caller path that bypasses resolve_session_id or a runtime that exports no id. Without it, a time-window join credits one agent's file-open to another agent's retrieval.",
      "ceiling": 1.0,
      "falsifier": "A runtime that exports no session id yields None BY DESIGN rather than a fabricated value. If session_rate stalls below 1.0, identify the runtime before assuming a bug."
    },
    "exposure_rate": {
      "definition": "clean reads carrying ranks and scores / clean reads",
      "computed_from": "memory-read rows, non-empty ranks",
      "baseline_2026_09_01": 0.02,
      "target": "equal to hit_rate",
      "target_rationale": "Needed to correct a later use-signal for position bias. The former gap -- memory_locator emitting 0.0% ranks because only memory_facade.recall ran the ranker -- was CLOSED on 2026-09-07 in eb425c8b; every locator row emitted since carries ranks and scores. The lifetime figure still shows the pre-fix rows, which is why this metric is scored on the rebaseline window.",
      "ceiling": "hit_rate",
      "falsifier": "Propensity cannot be reconstructed after the fact. Every read emitted without ranks is permanently undebiasable, so this metric only ever measures rows going forward.",
      "rebaseline_after": "2026-09-08T06:30:00Z",
      "rebaseline_commit": "eb425c8b \u2014 memory_locator emits ranks+scores"
    },
    "phase_rate": {
      "definition": "clean reads carrying a phase other than the 'unknown' sentinel / clean reads",
      "computed_from": "memory-read rows, phase not in (null, 'unknown')",
      "baseline_2026_09_01": 0.228,
      "target": 1.0,
      "target_rationale": "Resolved from the runtime like session_id, so anything below 1.0 is a caller that never passes one. memory_facade.recall hard-coded phase='unknown' and emitted 78% of all clean reads, so the store could not answer the first question anyone asks of it: which phase does memory actually help? Fixed 2026-09-08 by resolve_phase(); the metric exists so the answer stays measured rather than remembered.",
      "ceiling": 1.0,
      "falsifier": "A runtime that exports no phase yields the 'unknown' sentinel BY DESIGN. If phase_rate stalls, identify the calling runtime before assuming a bug -- and never count 'unknown' as attribution, which would report full coverage for rows that attribute nothing.",
      "rebaseline_after": "2026-09-08T07:30:00Z",
      "rebaseline_commit": "this run \u2014 memory_facade.recall resolves phase instead of hard-coding 'unknown'"
    },
    "use_rows": {
      "definition": "count of rate-eligible memory reads with a correlated memory-use row",
      "computed_from": "schema_version != 1.0, source == 'runtime', and memory-use.correlation_id joins one clean memory-read",
      "baseline_2026_09_01": 0,
      "target": "> 0 and growing from ORDINARY work, with zero manual seeding",
      "target_rationale": "The whole point. Until a clean retrieval has a correlated use record from unseeded traffic, memory cannot be ranked by usefulness, cannot be pruned, and no retrieval change can be judged against outcomes. Every other metric here is instrumentation waiting for this one.",
      "ceiling": "join-confirmed subset of hit reads",
      "falsifier": "A non-zero count produced by a human running the reconciler by hand proves the mechanism, NOT the loop. Check that matched reads carry session ids from sessions the human did not drive. The aggregate can report the mechanical count but cannot establish ordinary-work provenance or growth."
    }
  },
  "invariants": {
    "no_effect_on_use_rows": "An open proves INSPECTED, never HELPED. effect stays unset. Asserted by test_memory_effect and test_memory_reconcile.",
    "opens_never_feed_ranking": "No consumer of memory-use may write to the ranker or the pruner. Position bias makes opens partly a function of rank; feeding them back builds a rich-get-richer loop.",
    "no_negatives_fabricated": "A surfaced-and-unopened memory stays in the denominator and is never labelled 'ignored'. Silence is not evidence of non-use.",
    "tier_separation": "No rate is ever reported blended across tiers.",
    "test_telemetry_isolation": "BUILD_LOOP_TELEMETRY_SOURCE=test routes to a temp path; production store must show zero fixture rows."
  },
  "deferred_with_reasons": {
    "raise_default_limit": "Spends tokens and latency on every read with no feedback signal to say whether slots 11-26 are ever used. Revisit after use_rows > 0.",
    "embeddings": "The current lexical oracle structurally cannot evaluate them: it penalises exactly what embeddings add, topical match without literal terms. Needs ground truth first.",
    "pruning": "No usefulness signal to prune BY. Would fall back on recency, which was removed from ranking for scoring P@1 0.030."
  },
  "how_to_audit": [
    "python3 scripts/memory_store_stats.py --check-targets   # scored on the rebaseline window",
    "python3 scripts/memory_health.py --since 2026-09-01T07:00:00Z",
    "python3 scripts/memory_health.py                        # lifetime, for comparison",
    "python3 scripts/memory_reconcile.py                     # dry-run, all strategies",
    "python3 -m pytest scripts/test_memory_*.py scripts/test_content_index.py -q"
  ],
  "rebaseline_after": "2026-09-01T07:00:00Z",
  "rebaseline_rationale": "The ledger is append-only, so a lifetime rate can never fall below its own history: joinable_rate, session_rate and exposure_rate were all met by the code emitting rows on 2026-09-08 while the lifetime figure still read 'below' from pre-fix rows. Scoring a window makes a landed fix visible. This timestamp is the hour after ddb2dad8 (2026-08-31T23:29 PDT), the last of the four commits that added returned_paths, ranks, scores and session_id to the read row. Move it forward only when a NEW emission fix lands, and say which commit in this field. A metric may also carry its own `rebaseline_after`, which wins over this global one: emission fixes land per caller on different days, so one shared window either scores a metric against rows written before its own fix or shrinks to the newest fix and starves every other metric of rows. Expect a freshly re-baselined metric to read low on a handful of rows and converge as traffic accumulates -- that is the window filling, not a regression, and it is not a reason to move the point again."
}
