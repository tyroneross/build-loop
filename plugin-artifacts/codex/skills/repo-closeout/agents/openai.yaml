interface:
  display_name: "Repository Maintenance"
  short_description: "Audit repo architecture, artifacts, and closeout"
  default_prompt: "Use $build-loop:repo-closeout to classify the application and build profile, audit repository structure and artifacts, and close completed local work safely."
