---
id: <BUIL-AREA-slug>        # generated by `backlog.py add`; never hand-assigned
schema_version: 1
title: <one-line imperative summary>
status: open                # open | in-progress | blocked | deferred | done | dropped
priority: P2                # P0 | P1 | P2 | P3
type: debt                  # feature | fix | debt | infra | decision | cleanup | research
area: general               # free-form grouping key (security, ci, docs, …)
bucket: planned             # planned | initiative | decision  — see below
workstream: ""              # the branch/worktree this item belongs to; decision items
                            # surface only when their workstream is the active one
related_to: []              # ids of items this one depends on or informs
decision_options: []        # `decision` bucket only: the user-readable choices
decision_impacts: []        # `decision` bucket only: consequence per option
entities: [<path/to/file.py>]   # code this item touches
gated: none                 # none | prod-deploy | db-migration | infra | product-decision
provenance:
  source: <issue id / review finding / user request / run id>
  ref: null
evidence: [<test::case or observed failure — a cite, not a claim>]
supersedes: null
superseded_by: null
created: <YYYY-MM-DD>
validated:                  # YYYY-MM-DD the premise was last RE-CHECKED against the
                            # live repo. Empty = never; falls back to `created`.
                            # Stamped ONLY by `premise_revalidation.py validate
                            # --note "<evidence>"`, never by hand.
updated: <YYYY-MM-DD>
review_by: <YYYY-MM-DD>     # TTL — past this date the item is surfaced as stale
owner: unassigned
---

## Problem
<what's broken / missing, with evidence — a code cite or observed failure, not a cited statistic>

## Proposed fix
<smallest mechanism that addresses the root cause; prefer extend/delete over add>

## Acceptance
- <verifiable condition 1>
- <verifiable condition 2>

<!--
THIS TEMPLATE IS DERIVED, NOT AUTHORED. The field list above mirrors
`scripts/backlog.py` FIELD_ORDER exactly, and `scripts/schema_consistency_lint.py`
fails when the two drift apart.

Why that lint exists: this file previously documented a DIFFERENT schema
(`classify`, `effort`, `repo`, `branch`, `product_impacting`, `impact`, `source`)
than the one the CLI writes. 49 items in this repo carried the CLI shape while 17
in another repo carried this one with every `classify:` blank — because the
template told the author to fill a field no code reads. Each population looked
fine on its own.

Prefer `python3 scripts/backlog.py add …` over copying this file. The CLI
validates every enum, generates the id, and stamps the dates. Hand-authoring is
how the two schemas diverged in the first place.

BUCKET semantics (scripts/backlog.py BUCKET_VALUES, references/backlog-system.md):
  planned     known future work outside the queue. Selected only at a planning
              boundary; `promote` writes a queue receipt.
  initiative  major redesign or overhaul. NEVER auto-picked. Requires an explicit
              user-approval receipt, runs in an isolated non-main worktree, and
              is prohibited from deploying to production.
  decision    a question owed to the user, with decision_options/decision_impacts.
              Surfaces for its matching workstream while unrelated work continues.

Phase 5 NEVER drains `.build-loop/backlog/items/`. The active execution queue is
`.build-loop/queue/` — backlog items reach it only via `promote`.

Premise freshness: an item is surfaced for execution long after it was written. By
then the bug may be fixed, the file moved, or the precondition false. Past the
window (default 7 days) the drain gate refuses the item with
`stale_needs_revalidation` rather than manufacturing fake progress.
-->
