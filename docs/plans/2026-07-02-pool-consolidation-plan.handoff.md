<!-- SPDX-FileCopyrightText: 2025-2026 Tyrone Ross, Jr <46267523+tyroneross@users.noreply.github.com> | SPDX-License-Identifier: Apache-2.0 -->

# Handoff: pool-consolidation (increments 2–9)

Implementer contract per increment. Full context: `docs/plans/2026-07-02-pool-consolidation-plan.md`. Increments are STRICTLY SEQUENTIAL; each runs the Increment Protocol (worktree → fresh grep → edit → regenerate inventories → tests → Codex audit → rebase → re-grep → ff → push).

**Universal rules:** never hand-edit `plugin-artifacts/codex/**` (regenerates via `npm run codex:build-artifact` / commit hook); never rewrite historical docs (append only); the fresh grep at execution time is the binding ref list, not the plan's snapshot; every fold preserves ALL content (every H2 of the source must appear in the target — T-08).

- **When implementing F-02 (verify-dispatch fold):** satisfy T-01/T-05/T-06/T-08. The critical step is activation wiring — add the post-dispatch pointer in `agents/build-orchestrator.md` AND `skills/build-loop/SKILL.md`; a fold without wiring ships a dormant reference file (known build-loop failure class).
- **When implementing F-03 (knowledge-review fold):** satisfy T-03/T-08. `knowledge` stays `user-invocable: true`. Update `skills/auto-decision-capture/SKILL.md:370` and the CLAUDE.md internal-modes list; CLAUDE.md edits must keep `methodology_drift_lint.py --strict` green.
- **When implementing F-04 (logging-tracer-bridge fold):** read ADR-02 first; satisfy T-02. Fold the `availablePlugins.claudeCodeDebugger` preflight + Tier-1 inline helpers verbatim into `skills/logging-tracer/SKILL.md`, update `.claude-plugin/plugin.json:4` + `.claude-plugin/marketplace.json` descriptions, docstring in `scripts/test_bridge_preflight.py`, and ADD the `NON_BRIDGE_PREFLIGHT_SKILLS = {"logging-tracer"}` assertion.
- **When implementing F-05 (debugging cluster fold):** read ADR-01 first; satisfy T-01/T-04/T-08. Op-routing interface `{op: search|store|assess}`; detail into `skills/debugging-memory/references/{search,store,assess}.md` with provenance comments. The ★ load-bearing redirects (must not break): `agents/{api,frontend,database,performance}-assessor.md`, `references/memory-systems.md:217,219`, `references/phase-gate-checklist.md:230`, `references/iterate-protocol.md:42`, `skills/build-loop/references/phase-5-iterate.md:53`, `skills/debug-loop/SKILL.md:220`. Also: `scripts/sync_skills.py:36` SKILL_TREES, `skills/sync-skills/SKILL.md:122-124`, `tests/test_capability_registry.py:408-410` fixture, CLAUDE.md §Native Architecture & Debugging Skills. One logical commit (clean revert). Preserve the strict direct-apply gate and the Sonnet-pin on assess fan-out verbatim.
- **When implementing F-06 (loop-builder rename):** update `scripts/test_loop_builder.py:17-18` and the internal `skills/loop-builder/scripts/...` path refs at SKILL.md:40,79,85,91. Keep `loop_builder.py` filename.
- **When implementing F-07 (description pass):** read ADR-03. Additive-only edits to `description:` frontmatter lines; never remove existing trigger phrases. Pairs: 4 bridges, model-bakeoff↔model-tiering, root-cause-analysis↔debug-loop↔debugging-memory.
- **When implementing F-08 (script classification):** read ADR-04 and Q-01 — READ `scripts/script_relevance.py` before running it; never guess flags. Packet goes to `build-loop-memory/projects/build-loop/`, not this repo. Human approval gate is hard (T-09).
- **When implementing F-09 (script actions):** only approved rows; re-grep each cull target across `hooks/hooks.json`, `package.json` (files + scripts), `.github/workflows/`, `skills/`, `agents/`, script imports at execution time; batch headers by directory; satisfy T-05 + import/hook lints.

**Do NOT touch:** security triad, ui-design/ui-validator, `skills/architecture/*` + architecture-scout, auto-finding-capture, debug-loop, any agent file except the four assessor redirects in F-05 and the orchestrator pointer in F-02.
